c_learning/
├── 01_basics/
│   ├── hello_world.c           # Классическая первая программа
│   ├── comments_and_syntax.c   # Комментарии и базовый синтаксис
│   └── compilation_process.md   # Файл с описанием процесса компиляции (этот текст)
│
├── 02_datatypes/
│   ├── integers.c              # Целочисленные типы (int, short, long...)
│   ├── floating_point.c        # Типы с плавающей точкой (float, double)
│   ├── characters.c            # Символьный тип (char)
│   └── sizeof_example.c        # Пример использования оператора sizeof
│
├── 03_operators/
│   ├── arithmetic.c            # Арифметические операторы (+, -, *, /, %)
│   ├── relational_logical.c    # Операторы сравнения и логики (==, &&, ||)
│   └── assignment_increment.c  # Операторы присваивания и инкремента (=, +=, ++)
│
├── 04_control_flow/
│   ├── if_else.c               # Условный оператор if-else
│   ├── switch_case.c           # Конструкция switch-case
│   ├── for_loop.c              # Цикл for
│   ├── while_loop.c            # Цикл while
│   └── do_while_loop.c         # Цикл do-while
│
├── 05_functions/
│   ├── function_basics.c       # Объявление, определение и вызов функции
│   ├── parameters_and_return.c # Функции с параметрами и возвращаемым значением
│   └── header_example.h        # Пример заголовочного файла (.h)
│   └── main_with_header.c      # Файл, использующий этот заголовок
│
├── 06_memory/
│   ├── pointers_basics.c       # Основы указателей (*, &)
│   ├── pointers_and_arrays.c   # Связь указателей и массивов
│   └── dynamic_allocation.c    # Динамическое выделение памяти (malloc, free)
│
├── 07_preprocessor/
│   ├── define_macro.c          # Макросы #define
│   ├── conditional_comp.c     # Условная компиляция (#ifdef, #ifndef)
│   └── include_example.c       # Директива #include
│
└── 08_project_example/
    ├── Makefile                # Пример файла для автоматической сборки (make)
    ├── main.c                  # Главный файл проекта
    ├── utils.c                 # Вспомогательные функции
    └── utils.h                 # Заголовок для utils.c
