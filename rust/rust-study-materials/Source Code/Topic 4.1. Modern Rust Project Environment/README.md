# Topic 4.1. Modern Rust Project Environment

В этом каталоге лежит маленький, но законченный пример Rust-проекта с
минимальным циклом качества:

- `cargo fmt`;
- `cargo clippy`;
- `cargo test`;
- `cargo doc`.

Также показан вариант с alias-командами в `.cargo/config.toml`.
