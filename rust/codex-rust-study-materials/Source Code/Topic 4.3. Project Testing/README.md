# Тема 4.3. Тестирование проекта

Этот каталог показывает три уровня тестирования в Rust:

- unit tests в `src/lib.rs`;
- integration tests в каталоге `tests/`;
- doc tests в документационных комментариях.
