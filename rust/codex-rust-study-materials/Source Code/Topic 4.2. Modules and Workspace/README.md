# Topic 4.2. Modules and Workspace

В этом примере приложение разделено на два crates:

- `task_tracker_core` - доменная логика и форматирование;
- `task_tracker_cli` - консольный вход.

Такой вариант хорошо показывает границу между библиотечным кодом и интерфейсом
запуска.
