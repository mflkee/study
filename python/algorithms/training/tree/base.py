class TreeNode:
    def __init__(self, val = 0, left = None, right = None):
        self.val = val
        self.left = left
        self.right = right

def exoplore_tree(root):
    if val = r

root = TreeNode(1)
root.left = TreeNode(2)
root.right = TreeNode(3)

print(root.val)
print(root.left.val)
print(root.right.val)
