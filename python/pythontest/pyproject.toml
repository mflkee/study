[project]
name = "pythontest"
version = "0.1.0"
description = "Dependencies recovered from the legacy virtualenv"
requires-python = ">=3.14"
dependencies = [
    "httpx==0.27.0",
    "python-telegram-bot==21.0.1",
]
